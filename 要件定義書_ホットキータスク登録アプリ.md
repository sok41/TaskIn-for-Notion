# 要件定義書：TaskIn for Notion

## 1. 概要

Windows上でホットキーを押すと小さな入力ウィンドウが表示され、タスク名（と任意で期限）を入力するだけで、あらかじめ設定したNotionデータベースにタスクとして自動登録される常駐アプリケーション「TaskIn for Notion」。

## 2. 目的

- 作業中に思いついたタスクを、Notionを開かずに即座に記録できるようにする
- 入力の手間を最小限にし、思考を中断せずにタスク化できるようにする

## 3. 開発・リポジトリ情報

| 項目 | 内容 |
|---|---|
| GitHubリポジトリ | https://github.com/sok41/TaskIn-for-Notion.git |

## 4. 技術スタック

| 項目 | 内容 |
|---|---|
| フレームワーク | Tauri（Rust）+ React + TypeScript |
| 実行環境 | Windows（デスクトップ常駐アプリ） |
| 外部連携 | Notion API（Integration Token使用） |
| 採用理由 | シンプルな機能に対して軽量・高速に動作させたいため、Electronではなくバイナリサイズ・メモリ使用量の少ないTauriを採用。フロントエンド（React+TypeScript）は既存の自作アプリ「Simple Launcher」の資産を流用しやすい構成とする |

## 5. 機能要件

### 5.1 ホットキー起動
- 任意のグローバルホットキーでアプリの入力ウィンドウを呼び出す
- デフォルトホットキーは`Ctrl+Alt+N`とする
- ホットキーはユーザーが設定画面から変更可能とする

### 5.2 入力ウィンドウ
- ホットキー押下でポップアップ形式の小さな入力ウィンドウを画面中央に表示する
- デザインは「Notion Native」案を採用する：Notion本体のページ作成画面に近い見た目（白背景、上部にアイコン＋アプリ名のキャプション、タスク名を大きめの太字で表示、区切り線の下に期限をピル形式で表示）とする
- 入力項目は以下の2つ：
  - **タスク名**（必須）：テキスト入力欄
  - **期限**（任意）：以下2方式を併用
    - テキスト直接入力（例：`YYYY-MM-DD`形式）
    - 「今日」「明日」などのクイック選択ボタン
- タスク名が未入力の場合は登録不可（バリデーション）とする
- 期限は未入力のままでも登録可能とする

### 5.3 Notionへの登録
- 入力内容を、事前に設定したNotionデータベースへ新規ページとして登録する
- 登録するプロパティ：
  - タスク名（タイトルプロパティ）
  - 期限（日付プロパティ、入力があれば設定）
- ステータスなど、上記以外のプロパティは登録しない（シンプル構成）

### 5.4 登録後の挙動
- 登録処理が成功したら、ウィンドウ内またはWindows通知で成功メッセージを表示する
- 成功メッセージ表示後、一定時間（例：1〜2秒）でポップアップウィンドウを自動的に閉じる
- 登録に失敗した場合はエラーメッセージを表示し、ウィンドウは閉じずに再入力・再送信できるようにする

### 5.5 設定・認証情報の管理
- Notion連携には以下2つの情報が必要であり、いずれも設定画面から登録できるようにする：
  - **Integration Token**：Notion側で作成したIntegrationの認証用シークレットキー
  - **データベースID**：登録先データベースのURLから抽出する（ユーザーはURLを貼り付け、アプリ側でIDを自動抽出する）
- 上記に加え、ユーザー側の作業として「対象データベースを当該Integrationと共有（Connect）する」設定がNotion側で必要となる。設定画面に、Integration作成からデータベース共有までの手順をテキストベースで簡潔に箇条書き案内する（凝ったUIは不要）
- 保存した認証情報はローカルに平文で保持する（個人利用を前提とした当面の方針）
- 初回起動時、またはトークン未設定時は設定画面へ誘導する

### 5.6 スタートアップ自動起動
- デフォルトでWindows起動時にアプリを自動起動する設定とする
- 設定画面からON/OFFを切り替え可能とする

## 6. 非機能要件

- 常駐アプリとしてシステムトレイに常駐し、バックグラウンドで待機する
- ネットワーク未接続時など登録に失敗した場合、その旨をユーザーに明示する

## 7. 対象外（スコープ外）

- ステータス管理・進捗管理機能
- 複数のNotionデータベースからの選択機能（当面は登録先を1つに固定）
- タスクの編集・削除など、登録以外のNotion操作

## 8. 開発運用（Claude Codeスキル）

### 8.1 開発ログ記録スキル
- GitHubへのプッシュ作業を行った際、Claude Codeに手動で依頼することで実行するスキルを用意する
- スキル実行時の動作：
  - 対応するNotionページ（開発ログ用ページ：https://app.notion.com/p/sok4/Dev_TaskIn-3d711b69067880e2a30fd2ad7d0ba626 ）の配下に、新規の子ページを1件追加する（既存ページへの追記ではなく、対応内容ごとに新しいページを作成する形式）
  - 追加するページの内容には、今回の対応で「何を実装・修正したか」の要約を含める（コミットメッセージをそのまま転記するのではなく、変更内容の意図・概要をまとめた文章とする）
- 実行タイミングはあくまで手動依頼ベースとし、push操作自体をトリガーとした自動実行（GitHub Actions等）は当面採用しない

## 9. 今後の検討事項

- 配布（第三者への提供）を行う場合は、認証情報の保存方式を平文からWindows Credential Managerなどを用いた暗号化保存に切り替える
- 現状は1回のホットキー起動につきタスクを1件のみ登録する仕様だが、将来的には複数のタスクを連続して登録できるようにすることを検討する（例：登録後にウィンドウを閉じずに次の入力へ続けられる、など）
